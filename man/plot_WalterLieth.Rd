% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_WalterLieth.R
\name{plot_WalterLieth}
\alias{plot_WalterLieth}
\title{Create a Walter-Lieth climate diagram}
\usage{
plot_WalterLieth(
  X,
  diurnal = FALSE,
  obs_period = NULL,
  gcm = NULL,
  ssp = NULL,
  gcm_period = NULL
)
}
\arguments{
\item{X}{A \code{data.table} object produced using the function \code{\link[=plot_bivariate_input]{plot_bivariate_input()}}. This
table can include more models, scenarios, and variables than are used in individual calls to
\code{\link[=plot_bivariate]{plot_bivariate()}}.}

\item{diurnal}{Logical. If \code{TRUE}, the range between \code{Tmin} and \code{Tmax} is shown, representing the diurnal range for each month.}

\item{obs_period}{Character (optional). One of the observation periods (e.g., \code{"1961_1990"}).}

\item{gcm}{Character (optional). A specific global climate model (GCM). If unspecified, then the mean of all global climate models will be shown for the specified \code{ssp} and \code{gcm_period}.}

\item{ssp}{Character (optional). A Shared Socioeconomic Pathway scenario (SSP), required when a \code{gcm_period} is specified.}

\item{gcm_period}{Character (optional). A GCM simulation period (e.g., \code{"2041_2060"}), used for ensemble means or GCM-specific plots. Required when a \code{gcm_period} is specified.}
}
\value{
A \code{ggplot2} object showing the Walter-Lieth climate diagram.
}
\description{
This function creates a Walter-Lieth climate diagram, which summarizes mean monthly
temperature and precipitation to visualize seasonal climate patterns.
It supports both the classical version using monthly average temperature
and an enhanced version using monthly mean daily minimum (\code{Tmin}) and maximum (\code{tmax}) temperatures.
}
\details{
The diagram uses the following conventions:
\itemize{
\item \strong{Temperature}: red line showing mean monthly temperature. If \code{tmin} and \code{tmax} are supplied, they define a red ribbon showing the temperature range.
\item \strong{Precipitation}: blue line showing monthly precipitation, scaled at 2 mm per °C. For months >100 mm, the scale changes to 20 mm per °C to avoid diagram distortion.
\item \strong{Humid periods}: blue shading where precipitation exceeds temperature (P > T).
\item \strong{Dry periods}: red shading where temperature exceeds precipitation (T > P).
\item \strong{Wet periods}: dark grey shading where precipitation > 100 mm.
\item \strong{Freezing months}: marked with a blue polygon along the x-axis when temperature < 0°C.
}

This plot helps identify wet/dry seasons, frost months, and temperature-precipitation balance.

The Walter-Lieth diagram was introduced by Heinrich Walter and Helmut Lieth in the 1960s to provide a compact visual representation of climate normals. It is particularly valuable in ecology and biogeography for characterizing climate zones and inferring environmental conditions suitable for vegetation types or agricultural use.

Interpretation tips:
\itemize{
\item \strong{Shaded blue areas} indicate months where water supply exceeds potential evapotranspiration, often conducive to plant growth.
\item \strong{Shaded red areas} suggest moisture stress periods.
\item \strong{Shaded black areas} denote very wet months, which may correspond to tropical monsoon seasons or strong winter rains.
\item \strong{Polygon blocks on the baseline} signal freezing months and potential frost periods.
}
}
\examples{

\dontrun{
in_xyz <- data.frame(lon = -124, lat = 50.3557, elev = 291, id = 1)

# Obtain the input data for the plot
my_data <- plot_WalterLieth_input(in_xyz, 
                               obs_period = list_obs_periods()[1],
                               gcms = list_gcms()[c(1, 4, 5, 6, 7, 10, 11, 12)],
                               ssps = list_ssps()[2],
                               gcm_periods = list_gcm_periods()
                               )

# Plot 1961-1990 climate normals
plot_WalterLieth(my_data) 

# include the monthly diurnal range
plot_WalterLieth(my_data, diurnal = TRUE) 

# Plot 2001-2020 period climate normals
plot_WalterLieth(my_data, obs_period = list_obs_periods()[1])

# Plot a single GCM projection
plot_WalterLieth(my_data, gcm = "ACCESS-ESM1-5", ssp = "ssp245", gcm_period = "2041_2060")

# Plot ensemble mean projection for an SSP and period
plot_WalterLieth(my_data, ssp = "ssp245", gcm_period = "2041_2060")
}

}
