#' @param xyz a `terra::SpatRaster` with a single layer containing elevation values in metres, or a `data.frame` with the following columns
#' "long", "lat", "elev", and a unique "id". Any extra columns will be ignored and not output.
