#' @param max_run integer. Maximum number of model runs to include, not including the ensemble mean.
#'   Runs are included in the order they 
#'   are found in the models data until `max_run` is reached. Defaults to 0L.
