#' @param max_run integer. Maximum number of model runs to include.
#'   A value of 0 returns the `ensembleMean` only. Runs are included in the order they 
#'   are found in the models data until `max_run` is reached. Defaults to 0L.
