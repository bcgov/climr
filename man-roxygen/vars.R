#' @param vars character. A vector of climate variables to compute. 
#' Options are [`list_variables()`]. 
#' Definitions can be found by calling `View(variables)` and queried by loading the 'variables' object into the R environment with `data(variables)`. 
#' Defaults to monthly PPT, Tmax, Tmin.

