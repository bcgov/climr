#' @param ensemble_mean Logical. Return the mean of the individual GCM runs? If `ensemble_mean = TRUE` and `max_run = 0`, only
#' the mean will be returned. To return an individual run and exclude the mean, set `ensemble_mean = FALSE` and `max_run = 1`.
